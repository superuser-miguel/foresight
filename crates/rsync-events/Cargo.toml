[package]
name = "rsync-events"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Parse rsync 3.4.x output into structured events (pure, no GTK)"

[dependencies]
regex = "1"
once_cell = "1"
